# Описание доступа к graylog2 серверу для отправки логов по протоколу UDP GELF
# Если указана пустая строка, то отправка в graylog сервер не включается
# Формат: proto://ip.address.or.domain.name:port
# Пример: tcp://127.0.0.1:12201
# Default value: ""
Graylog: "udp://127.0.0.1:12221"
